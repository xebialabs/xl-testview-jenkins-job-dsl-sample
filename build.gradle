
defaultTasks 'libs'

repositories {
  jcenter()
}

configurations {
  libs
}

dependencies {
  libs 'org.codehaus.groovy.modules.http-builder:http-builder:0.7.2'
}

task clean(type: Delete) {
  delete 'lib'
}

task libs(type: Copy) {
  into 'lib'
  from configurations.libs
}
